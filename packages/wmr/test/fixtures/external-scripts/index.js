// @ts-ignore-next
// eslint-disable-next-line import/extensions
import { h, render } from '//unpkg.com/preact?module';

render(h('pre', null, 'rendered from unpkg'), document.getElementById('external'));
